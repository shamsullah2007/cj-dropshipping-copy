kind = "web"
previewPath = "/"
title = "CJ Dropshipping"
version = "1.0.0"
id = "artifacts/cj-dropshipping"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20357

[services.development]
run = "pnpm --filter @workspace/cj-dropshipping run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/cj-dropshipping", "run", "build" ]
publicDir = "artifacts/cj-dropshipping/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20357"
BASE_PATH = "/"
