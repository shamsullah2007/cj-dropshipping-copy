IMPORTANT — READ BEFORE MAKING ANY CHANGES:
The Checkout / Cart page (Step 1 of 4) is already built and working — do 
NOT modify it, its layout, its components, or its styling in any way. 
Do NOT touch any other existing page, route, model, or auth logic either.

Your task is ONLY to build the remaining 3 steps of the checkout flow as 
NEW pages/routes:
- Step 2: Shipping
- Step 3: Payment
- Step 4: Confirmation

Before writing any code:
1. Open the existing Cart/Checkout page and use it as the exact visual and 
   structural reference — same header, same 4-step progress indicator style 
   (numbered circles with orange for active step), same Order Summary 
   sidebar on the right, same fonts, spacing, buttons, and card styling.
2. Confirm how cart data currently flows (state, session, or database) so 
   Shipping/Payment/Confirmation can access the same cart items and totals 
   instead of using disconnected mock data.
3. Reuse the existing mobile-responsive system already applied across the 
   site — don't introduce a new one.

PAGE 2 — SHIPPING:
- Keep the same top progress bar, now with step 2 "Shipping" highlighted 
  in orange and step 1 "Cart" marked as completed (checkmark style).
- Let the user select a saved address (reuse the existing Address Book 
  component/data if already built) or add a new address via a form.
- Show shipping method options (e.g., Standard, Expedited) each with 
  price and estimated delivery date, selectable via radio buttons or cards.
- Order Summary sidebar (right side, same style as Cart page) updates 
  live with shipping cost added to the total.
- "Back" button to return to Cart, "Continue to Payment" button to proceed.

PAGE 3 — PAYMENT:
- Same progress bar, step 3 "Payment" highlighted, steps 1–2 marked completed.
- Let the user select or add a payment method (reuse existing Wallet/
  Payment Methods component if already built) — support card and/or 
  wallet balance depending on what's already integrated.
- Show final Order Summary sidebar with items, shipping, tax, and total.
- Basic validation before allowing "Place Order" (no empty required fields).
- "Back" button to return to Shipping, "Place Order" button to confirm 
  and proceed.

PAGE 4 — CONFIRMATION:
- Same progress bar, step 4 "Confirmation" highlighted, all previous 
  steps marked completed.
- Show a success message/icon, order number, full order summary (items, 
  shipping address, payment method used, shipping method, total paid), 
  and estimated delivery date.
- "Track Order" button linking to the existing Order Tracking page.
- "Back to My Orders" button linking to the existing Order Status page.
- "Continue Shopping" button linking back to the homepage.

MOBILE RESPONSIVE REQUIREMENTS:
- All 3 new pages must be fully responsive at mobile (~375–480px), tablet 
  (~768px), and desktop (1024px+), matching the responsive behavior already 
  applied elsewhere on the site.
- On mobile, the numbered progress bar should shrink to a compact version 
  (e.g., "Step 2 of 4: Shipping") instead of showing all 4 full circles 
  with labels.
- Order Summary sidebar should move below the main content and become 
  collapsible/expandable on mobile instead of sitting fixed on the side.
- Forms (address, payment) must stack fields vertically with full-width 
  inputs on mobile, touch targets at least ~44px.
- "Continue" / "Place Order" buttons should stay easily reachable, ideally 
  sticky at the bottom of the screen on mobile.

REQUIREMENTS:
- Match the Cart page's exact visual language — same orange accent color, 
  same card/border styling, same typography — so the whole flow feels 
  seamless.
- Reuse existing components (address cards, payment cards, buttons) instead 
  of duplicating them.
- Use mock/dummy data only where a real backend/payment integration isn't 
  available yet, but structure it so it's easy to wire to a real API later.
- Do not alter the Cart page or any other existing file.
- After building, list every new file created and confirm the Cart page 
  and all other existing pages were left untouched.

If completing the real order-placement logic requires touching existing 
backend files, stop and ask me first before making any changes.